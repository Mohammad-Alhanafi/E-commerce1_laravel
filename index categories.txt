<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>قائمة الفئات الذهبية</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="{{ asset('css/categories.css') }}">
</head>
<body>
 <div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-list"></i> قائمة الفئات الذهبية</h2>
        <a href="{{ route('categories.create') }}" class="btn btn-gold">
            <i class="fas fa-plus-circle"></i> إضافة فئة جديدة
        </a>
    </div>

    @if($categories->count() > 0)
        <!-- Table -->
        <div class="table-responsive">
           <table class="table table-hover text-center align-middle table-header-gold">
                <thead class="table-header-gold">
                    <tr>
                        <th style="color: black">#</th>
                        <th style="color: black">اسم الفئة</th>
                        <th style="color: black">الوصف</th>
                        <th style="color: black">الحالة</th>
                        <th style="color: black">عدد المنتجات</th>
                        <th style="color: black">الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($categories as $category)
                    <tr>
                        <td>{{ $category->id }}</td>
                        <td class="fw-bold" style="color: #d4af37;">{{ $category->name }}</td>
                        <td>{{ $category->description }}</td>
                        <td>
                            @if($category->status == 1)
                                <span class="status-badge status-active">نشط</span>
                            @else
                                <span class="status-badge status-inactive">غير نشط</span>
                            @endif
                        </td>
                        <td class="fw-bold" style="color: #d4af37;">{{ $category->products_count ?? 0 }}</td>
                        <td>
                            <div class="action-buttons">
                                <a href="{{ route('categories.show', $category->id) }}" class="btn btn-sm btn-info" title="عرض">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a href="{{ route('categories.edit', $category->id) }}" class="btn btn-sm btn-warning" title="تعديل">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form method="POST" action="{{ route('categories.destroy', $category->id) }}" class="d-inline">
                                    @csrf
                                    @method('DELETE')
                                    <button class="btn btn-sm btn-danger" title="حذف" onclick="return confirm('هل أنت متأكد من الحذف؟')">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    @endforeach
                </tbody>
            </table>
        </div>


        <br>

    <div>
    <ul class="pagination justify-content-center">
        {{-- Previous Page --}}
        @if ($categories->onFirstPage())
            <li class="page-item disabled">
                <span class="page-link"><i class="fas fa-chevron-right"></i></span>
            </li>
        @else
            <li class="page-item">
                <a class="page-link" href="{{ $categories->previousPageUrl() }}">
                    <i class="fas fa-chevron-right"></i>
                </a>
            </li>
        @endif

        {{-- Page Numbers --}}
        @foreach ($categories->getUrlRange(1, $categories->lastPage()) as $page => $url)
            <li class="page-item {{ $categories->currentPage() == $page ? 'active' : '' }}">
                <a class="page-link" href="{{ $url }}">{{ $page }}</a>
            </li>
        @endforeach

        {{-- Next Page --}}
        @if ($categories->hasMorePages())
            <li class="page-item">
                <a class="page-link" href="{{ $categories->nextPageUrl() }}">
                    <i class="fas fa-chevron-left"></i>
                </a>
            </li>
        @else
            <li class="page-item disabled">
                <span class="page-link"><i class="fas fa-chevron-left"></i></span>
            </li>
        @endif
    </ul>
</div>


    @else
        <!-- Empty State -->
        <div class="text-center py-5">
            <i class="fas fa-folder-open fa-3x mb-3" style="color: #d4af37;"></i>
            <h4>لا توجد فئات حتى الآن</h4>
            <p>أضف فئة جديدة للبدء.</p>
            <a href="{{ route('categories.create') }}" class="btn btn-gold mt-2">
                <i class="fas fa-plus-circle"></i> إضافة فئة جديدة
            </a>
        </div>
    @endif

</div>















    :root {
            --primary-gold: #D4AF37;
            --dark-gold: #B8860B;
            --light-gold: #FFD700;
            --pale-gold: #F5E8B0;
            --rich-gold: #C5A028;
            --primary-black: #0A0A0A;
            --secondary-black: #1A1A1A;
            --light-black: #2A2A2A;
            --charcoal: #333333;
            --dark-charcoal: #222222;
            --white: #FFFFFF;
            --off-white: #F5F5F5;
            --gradient-gold: linear-gradient(135deg, #D4AF37 0%, #FFD700 50%, #B8860B 100%);
            --gradient-dark: linear-gradient(135deg, #0A0A0A 0%, #1A1A1A 50%, #2A2A2A 100%);
        }
        
        * {
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: var(--primary-black);
            color: var(--pale-gold);
            direction: rtl;
            min-height: 100vh;
            overflow-x: hidden;
            padding: 20px;
        }
        
        /* Table Container */
        .table-container {
            background: var(--secondary-black);
            border-radius: 18px;
            padding: 30px;
            margin: 30px auto;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(212, 175, 55, 0.2);
            max-width: 1400px;
        }
        
        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(212, 175, 55, 0.2);
        }
        
        .table-title {
            color: var(--light-gold);
            font-weight: 700;
            font-size: 1.8rem;
            margin: 0;
            display: flex;
            align-items: center;
        }
        
        .table-title i {
            margin-left: 15px;
            font-size: 1.8rem;
            background: var(--gradient-gold);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        /* Button Styles */
        .btn-gold {
            background: var(--gradient-gold);
            color: var(--primary-black);
            border: none;
            padding: 12px 28px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 1.05rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3);
            position: relative;
            overflow: hidden;
            z-index: 1;
            text-decoration: none;
            display: inline-block;
        }
        
        .btn-gold::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 0;
            height: 100%;
            background: var(--gradient-dark);
            transition: width 0.4s ease;
            z-index: -1;
        }
        
        .btn-gold:hover::before {
            width: 100%;
        }
        
        .btn-gold:hover {
            color: var(--light-gold);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(212, 175, 55, 0.5);
        }
        
        .btn-gold i {
            margin-right: 8px;
            font-size: 1.1rem;
        }
        
        /* Table Styles */
        .table {
            color: var(--pale-gold);
            margin-bottom: 0;
        }
        
        .table thead {
            background: rgba(212, 175, 55, 0.1);
            border-bottom: 2px solid var(--primary-gold);
        }
        
        .table thead th {
            color: var(--light-gold);
            font-weight: 700;
            font-size: 1.1rem;
            padding: 18px 15px;
            border: none;
            text-align: center;
            vertical-align: middle;
        }
        
        .table tbody tr {
            transition: all 0.3s;
            border-bottom: 1px solid rgba(212, 175, 55, 0.1);
        }
        
        .table tbody tr:hover {
            background: rgba(212, 175, 55, 0.08);
            transform: scale(1.005);
        }
        
        .table tbody td {
            padding: 18px 15px;
            vertical-align: middle;
            text-align: center;
            border: none;
            font-weight: 500;
        }
        
        /* Status Badges */
        .status-badge {
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 0.9rem;
            display: inline-block;
        }
        
        .status-active {
            background: rgba(212, 175, 55, 0.2);
            color: var(--light-gold);
            border: 1px solid rgba(212, 175, 55, 0.4);
            box-shadow: 0 3px 10px rgba(212, 175, 55, 0.2);
        }
        
        .status-inactive {
            background: rgba(255, 255, 255, 0.1);
            color: var(--pale-gold);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        /* Action Buttons */
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
        }
        
        .btn-action {
            width: 42px;
            height: 42px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
            border: none;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            font-size: 1.1rem;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            cursor: pointer;
            text-decoration: none;
        }
        
        .btn-action:hover {
            transform: translateY(-5px) scale(1.1);
        }
        
        .btn-view {
            background: rgba(212, 175, 55, 0.15);
            color: var(--light-gold);
            border: 1px solid rgba(212, 175, 55, 0.3);
        }
        
        .btn-view:hover {
            background: rgba(212, 175, 55, 0.25);
            box-shadow: 0 6px 15px rgba(212, 175, 55, 0.3);
        }
        
        .btn-edit {
            background: rgba(255, 215, 0, 0.15);
            color: var(--light-gold);
            border: 1px solid rgba(255, 215, 0, 0.3);
        }
        
        .btn-edit:hover {
            background: rgba(255, 215, 0, 0.25);
            box-shadow: 0 6px 15px rgba(255, 215, 0, 0.3);
        }
        
        .btn-delete {
            background: rgba(184, 134, 11, 0.15);
            color: var(--dark-gold);
            border: 1px solid rgba(184, 134, 11, 0.3);
        }
        
        .btn-delete:hover {
            background: rgba(184, 134, 11, 0.25);
            box-shadow: 0 6px 15px rgba(184, 134, 11, 0.3);
        }
        
        /* Pagination */
        .pagination .page-link {
            background-color: rgba(212, 175, 55, 0.1);
            border: 1px solid rgba(212, 175, 55, 0.3);
            color: var(--pale-gold);
            margin: 0 3px;
            border-radius: 8px;
            transition: all 0.3s;
            text-decoration: none;
        }
        
        .pagination .page-link:hover {
            background-color: rgba(212, 175, 55, 0.2);
            color: var(--light-gold);
            transform: translateY(-3px);
        }
        
        .pagination .page-item.active .page-link {
            background: var(--gradient-gold);
            color: var(--primary-black);
            border-color: var(--primary-gold);
            font-weight: 700;
        }
        
        /* Image Styles */
        .category-img {
            width: 70px;
            height: 70px;
            object-fit: cover;
            border-radius: 50%;
            border: 2px solid var(--primary-gold);
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }
        
        ::-webkit-scrollbar-track {
            background: var(--secondary-black);
        }
        
        ::-webkit-scrollbar-thumb {
            background: var(--gradient-gold);
            border-radius: 5px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: var(--primary-gold);
        }
        
        /* Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .table-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 20px;
            }
            
            .table-title {
                font-size: 1.5rem;
            }
            
            .table th, .table td {
                padding: 12px 8px;
                font-size: 0.9rem;
            }
            
            .btn-action {
                width: 36px;
                height: 36px;
                font-size: 1rem;
            }
            
            .status-badge {
                padding: 6px 12px;
                font-size: 0.8rem;
            }
        }
        
        @media (max-width: 576px) {
            .table-container {
                padding: 15px;
            }
            
            .action-buttons {
                flex-direction: column;
                gap: 5px;
            }
            
            .btn-action {
                width: 32px;
                height: 32px;
            }
            
            .category-img {
                width: 50px;
                height: 50px;
            }
        }
    


        .table-responsive {
    margin-top: 20px; 
}

.table th, .table td {
    vertical-align: middle;
    padding: 12px 10px; 
}

.action-buttons .btn {
    margin: 0 2px;
}

.status-badge.status-active {
    color: #fff;
    background-color: #28a745;
    padding: 3px 8px;
    border-radius: 12px;
    font-size: 0.85rem;
}

.status-badge.status-inactive {
    color: #fff;
    background-color: #dc3545;
    padding: 3px 8px;
    border-radius: 12px;
    font-size: 0.85rem;
}

.table-container {
    padding: 20px;
    background: #fff; 
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0,0,0,0.05);
}


.table-header-gold thead {
    background-color: #d4af37; /* اللون الذهبي */
    color: white; /* نص أبيض للوضوح */
}

